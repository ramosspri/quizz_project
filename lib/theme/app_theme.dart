import 'app_colors.dart';
import 'app_text_styles.dart';

// Classe para pegar os temas do aplicativo
class AppTheme {
  static AppColors get colors => AppColorsDefault();
  static AppTextStyles get textStyles => AppTextStylesDefault();
}
